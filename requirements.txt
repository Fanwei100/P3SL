# Core deep-learning stack
torch>=2.0
torchvision>=0.15
numpy>=1.23
pandas>=1.5

# WebSocket transport between coordinator and client workers
websockets>=11.0
websocket-client>=1.5

# Dataset partitioning utilities
fedlab>=1.3
flwr-datasets>=0.1
datasets>=2.14

# Optional low-rank tensor decomposition (Tucker / PARAFAC) for legacy compression experiments
tensorly>=0.8

# Hardware power telemetry (Linux/Jetson only)
jetson-stats>=4.2  ; sys_platform == "linux"   # provides the `jtop` module
psutil>=5.9

# Misc utilities
matplotlib>=3.5
tqdm>=4.65
pytz>=2023.3
wakeonlan>=3.0
